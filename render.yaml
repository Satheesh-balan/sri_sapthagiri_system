services:
  - type: web
    name: sri-sapthagiri-inventory
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 5001
      - key: MONGO_URI
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
